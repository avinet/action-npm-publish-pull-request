{
  "name": "action-npm-publish-pull-request",
  "version": "1.0.1-beta.mkdsa.0",
  "description": "Publishes pre-release packages for pull requests.",
  "main": "src/index.ts",
  "scripts": {
    "build": "tsc && ncc build",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Asplan Viak AS",
  "license": "ISC",
  "dependencies": {
    "@actions/core": "^1.9.1",
    "@actions/exec": "^1.1.1",
    "@actions/github": "^5.0.0",
    "@types/node": "^18.0.3",
    "@vercel/ncc": "^0.34.0",
    "typescript": "^4.7.4"
  }
}
